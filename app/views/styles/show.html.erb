<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @style.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @style.description %>
</p>

<div>
  <% if @style.beers.empty? %>
      No beers for this style
  <% else %>
      <ul>
        <% @style.beers.each do |beer| %>
            <li> <%= link_to beer.name, beer %> </li>
        <% end %>
      </ul>
  <% end %>
</div>

<%= edit_and_destroy_buttons(@style) %>